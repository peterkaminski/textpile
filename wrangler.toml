name = "textpile"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"

# Durable Objects (reference the separate DO Worker)
[durable_objects]
bindings = [
  { name = "POST_ID_ALLOCATOR", class_name = "PostIdAllocator", script_name = "textpile-do" }
]
